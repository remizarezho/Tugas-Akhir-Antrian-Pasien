<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tugasakhirantrianpasien">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailPoli"></activity>
        <activity android:name=".panggil_nomor"></activity>
        <activity android:name=".profile" />
        <activity android:name=".nomor_antrian" />
        <activity android:name=".activity_informasi" />
        <activity android:name=".jadwal" />
        <activity android:name=".PoliKlinik" />
        <activity android:name=".LihatDataPasien" />
        <activity android:name=".tambah_data_pasien" />
        <activity
            android:name=".Nav_Home"
            android:label="@string/title_activity_nav__home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LupaPassword" />
        <activity android:name=".SignUp" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>